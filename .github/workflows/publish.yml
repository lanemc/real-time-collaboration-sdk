name: Publish to NPM

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'
          
      - name: Install dependencies
        run: npm install --legacy-peer-deps
        
      - name: Build packages
        run: |
          # Build packages in dependency order
          echo "Building @rtc-sdk/core..."
          cd packages/core && npm run build
          
          echo "Building @rtc-sdk/client-web..."
          cd ../client-web && npm run build
          
          echo "Building @rtc-sdk/server..."
          cd ../server && npm run build
          
          echo "Building @rtc-sdk/react-hooks..."
          cd ../react-hooks && npm run build
        
      - name: Publish to NPM
        run: |
          set -e
          
          # Publish packages in dependency order
          echo "Publishing @rtc-sdk/core..."
          cd $GITHUB_WORKSPACE/packages/core && npm publish --access public
          
          echo "Publishing @rtc-sdk/client-web..."
          cd $GITHUB_WORKSPACE/packages/client-web && npm publish --access public
          
          echo "Publishing @rtc-sdk/server..."
          cd $GITHUB_WORKSPACE/packages/server && npm publish --access public
          
          echo "Publishing @rtc-sdk/react-hooks..."
          cd $GITHUB_WORKSPACE/packages/react-hooks && npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN_SAAS_DEV_KIT }}