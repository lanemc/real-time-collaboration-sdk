{
  "name": "real-time-collaboration-sdk",
  "version": "0.1.2",
  "description": "A comprehensive SDK for adding real-time collaborative editing to web and mobile applications",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/real-time-collaboration-sdk.git"
  },
  "keywords": [
    "collaboration",
    "real-time",
    "operational-transform",
    "websocket",
    "multiplayer",
    "sdk"
  ],
  "author": "Your Name",
  "license": "MIT"
}